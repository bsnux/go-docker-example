# Testing build image for CI/CD system

This is a simple containerized golang app for testing build image process from
a CI/CD system.

## Instructions

Add following line to your system:

```
$ bash -c "docker build -t bsnux/hello:latest ."
```
